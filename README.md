# Arch-Installation-Script
This script is to install arch very easily, it has less functions and it installs only base arch using its wiki.
# WARNING: It only works if disk is /dev/sda right now and I didn`t test it right now
